@using OzelDersYerim.Web.Models.Dtos;
@model List<StudentLessonListDto>
   <div class="row mt-3 mb-4">
    
     <div>
         @await Component.InvokeAsync("Branches")
     </div>     
@if (User.Identity.IsAuthenticated)
{
    @if (User.IsInRole("Teacher"))
    {
        <div class="container">
         <div class="col-12 mt-3">
          <h1  class="display-6 text-center">Ders Almak İsteyen Öğrenciler</h1>
        @foreach (var les in Model)
        {
            @await Html.PartialAsync("_StudentLessonsPartial", les)
            
        }
      
     
    </div>
     </div>

    }
}
     </div> 